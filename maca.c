#include<stdlib.h>
#include<stdio.h>
int main(){
    int m;
    float pay;
    printf("insira a quantidade de macas:");
    scanf("%f",&v);
    if(m<12)}
       pay=1.30*m;
       printf("A quantia total a pagar e %f\n",pay);
       }
    else{
         pay=1.10*pay;
         printf("A quantia total a pagar e %f\n",pay);
    system("pause");
    return 0;
}
